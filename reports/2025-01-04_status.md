# Status Summary — 2025-01-04

## Goal
Recreate sub‑day MAE temporal molt regressor; add streaming and new data.

## Work completed
- **Env/setup**: New venv (.venv) on Py3.12; installed pandas/tqdm/FastAPI/uvicorn/torch/ultralytics/seaborn/matplotlib; downgraded numpy to 1.26.4 for torch/vision compatibility.
- **FastAPI app**: Added `app_fastapi.py` with `/predict`, `/predict_stream`, `/health`, `/ui`; camera streaming in `templates/index.html` (MediaStream → `/predict_stream`, thumbnail/bbox overlay). Optional YOLO detector via `YOLO_MODEL_PATH`.
- **Manifests**:
  - `tools/build_manifest.py` (full + 2016-only).
  - `data/processed/manifest.csv` (all years, 2767 rows, 316 labeled) and `manifest_2016.csv` (444 rows, 0 labeled via manifest).
  - Regenerated 2016 dataset via `GreenCrabDataLoader` → `data/processed/crab_dataset.csv` (425 rows, 230 labeled, 18 crabs).
- **Crate .docx extraction**: `tools/extract_crate_docx.py` → images in `data/processed/crate_images/...` and metadata `data/processed/crate_docs.csv` (108 records).
- **Merge crate data**: `tools/merge_crate_data.py` maps degree_of_molt to approximate days (imminent=0.5d, late=4d, mid=9d, new=16d) and merges with 2016 → `data/processed/crab_dataset_merged.csv` (909 rows, 713 labeled, 126 crabs).
- **Temporal training (current code)**:
  - 2016 only (labels from loader): best MAE ~4.28d, R² ~0.45 (window 5).
  - Merged (crate heuristics): best MAE ~3.97d, R² ~0.41 (window 5).
  - Artifacts: `models/best_temporal_model.pkl`, `models/temporal/`, `plots/temporal_vit_evaluation.png` reflect the latest run.
- **ViT feature extraction**: `tools/extract_vit_features_with_columns.py` created `data/processed/crab_dataset_2016_vit.csv` with feature_* columns; temporal retrain on it yields best MAE ~4.9d.
- **Stored artifacts inspection**: `models/temporal/*.pkl` and `results.json` show MAE ~4–5d, not the claimed ~0.5d.

## Observations on sub‑day MAE
- Past docs (PRESENTATION_README.md, CVPR tex) claim Temporal RF/GB MAE ~0.5d on ~230 samples (11 crabs). No preserved artifact or script currently reproduces this; cached models are ~4–5d.
- Temporal scripts in repo use minimal temporal features unless image features are explicitly embedded; label quantity/quality is limited (230 true labels in 2016; crate labels are heuristic).

## Outstanding issues
- Base ViT regressor on merged set crashes in sandbox; needs local run outside sandbox.
- Sub‑day MAE not reproducible with current code/labels; likely depended on a different feature pipeline or labels not preserved.

## Next steps (when back)
1) If available, locate original sub‑day artifacts (feature caches/model pickles) or exact script/split used.
2) Add richer temporal features with aggregated ViT stats (mean/std/delta/trend, PCA) and rerun crab-level splits to probe MAE further.
3) Improve labels: replace crate heuristics with actual molt dates if obtainable; re-run temporal and base regressors.
4) If needed, re-run base ViT regressor on merged set locally (outside sandbox) using the provided command to measure single-image MAE.

